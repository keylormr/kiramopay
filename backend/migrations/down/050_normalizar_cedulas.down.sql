-- Sin reversa: la normalizacion descarta los separadores originales y no hay
-- forma de reconstruirlos. Dejar las cedulas en su forma canonica es correcto
-- tambien para el codigo anterior (buscaba el valor tal cual se registrara).
SELECT 1;
